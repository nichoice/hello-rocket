table! {
    users (id) {
        id -> Nullable<Integer>,
        username -> Text,
        email -> Text,
        password -> Text,
        // created_at -> Timestamp,
        // updated_at -> Timestamp,
    }
}
